
spring:
  application:
    name: pacificOcean
  datasource:
    url: jdbc:postgresql://localhost:5432/
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    show_sql: true
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        format_sql: true
